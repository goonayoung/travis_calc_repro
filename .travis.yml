
dist: xenial
language : python
python:
    - "3.7"

virtualenv:
  system_site_packages: true

install:
 - pip install --user --upgrade pip
 - pip install python3-pyqt5
 - pip install openpyxl
 - pip install pandas
 - pip install numpy
 - pip install sklearn



script : python TDD_GUI_Setting.py
